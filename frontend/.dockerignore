# Keep the build context small and secrets out of the image entirely.
node_modules
dist
.env
.env.*
!.env.example
*.log
coverage
test-results
playwright-report
.git

# Regenerated during the build by `npm run routes`.
src/routeTree.gen.ts
